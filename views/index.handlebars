<div class="container-fluid">
    <div class="row">
        <div class="col-sm-2"></div>
        <div class="col-sm-8 mt-5">
            <img align="center" id="burgerImg" src="/assets/img/My_Burger.png" alt="hamburger" class="mx-auto d-block">
            <h1 align="center" class="mb-4">Eat Da Burger!!</h1>
        </div>
        <div class="col-sm-2"></div>
    </div>
    <div class="row">
        <div class="col-sm-2"></div>
        <div class="col-sm-4 mt-2">
            <h5 align="center">Devour your burger! &#128525</h3>
            <ul class="list-group">
                {{#each hamburgers}}
                    {{#unless devoured}} 
                        <li class="list-group-item">
                            <p class="d-inline-flex my-2">
                                {{burger_name}}</p>
                            <button class="devourBtn btn btn-success float-right" data-id="{{id}}">Devour it!</button>
                        </li>
                    {{/unless}}
                {{/each}}
            </ul>
        </div>
        <div class="col-sm-4 mt-2">
            <h5 align="center">Burgers Already eaten!!! &#128076</h5>
            <ul class="list-group">
            {{#each hamburgers}}
                {{#if devoured}}
                    <li class="list-group-item">
                        <p class="d-inline-flex my-2">
                            {{burger_name}}</p>
                        <button class="deleteBtn btn btn-danger float-right" data-id="{{id}}">Delete</button>
                    </li>
                {{/if}}
            {{/each}}
            </ul>
        </div>
        <div class="col-sm-2"></div>
    </div>
    <div class="row mt-4">
        <div class="col-sm-2"></div>
        <div class="col-sm-8">
            <h5 align="center">Request a new burger </h5>
            <form class="create-form">
                <div class="form-group mx-auto" align="center">
                    <div class="input-group mb-3">
                        <input type="text" class="form-control" placeholder="Burger name" aria-label="Recipient's username" aria-describedby="basic-addon2" id="burgerName">
                        <div class="input-group-append">
                            <button class="btn btn-dark" type="submit">Place your Order</button>
                        </div>
                    </div>
                </div>
            </form>
        </div>
        <div class="col-sm-2">
        </div>
    </div>

    <!-- Footer -->
    <div class="navbar fixed-bottom justify-content-center">&copy; App by Christian Kapita Mubakilay</div>

</div>